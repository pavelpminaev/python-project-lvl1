name: githab-actions
on: push
jobs:
        my-job:
                runs-on: macos-latest
                steps: 
                        - name: make lint
                          run:
                                pip install poetry
                                make lint
